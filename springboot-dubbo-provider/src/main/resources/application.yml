server:
  port: 8090
spring:
  application:
    name: czj-dubbo-provider
  dubbo:
    application:
      name: czj-dubbo-provider   #别名，提供方应用信息，用于计算依赖关系
    registry:
      address: zookeeper://10.211.55.3:2181 #使用zookeeper注册中心暴露服务地址
    protocol:
      name: dubbo
      port: -1         #配置暴露服务端口
    scan: com.chengzejun.user.service
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/playtest?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    userName: root
    password: 667788dog
    jpa:
      database: MySQL
      show-sql: true
      open-in-view: true